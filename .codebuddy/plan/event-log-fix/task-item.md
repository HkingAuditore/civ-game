# 实施计划：事件日志显示修复

所有任务均在 `src/logic/simulation.js` 单文件中完成。

---

- [ ] 1. 修复模板字符串插值损坏（`?{` → `${`）
  - 全局搜索 `simulation.js` 中所有 `?{` 并替换为 `${`，确保模板字符串变量能正确插值
  - 涉及行：约 3411、5235、5257、5901、6234、6254 行
  - 替换后逐一确认每处模板字符串的反引号（`` ` ``）包裹完整，语法无误
  - _需求：1.1、1.2、1.3、1.4、1.5_

- [ ] 2. 修复 emoji 截断（`??` → `⚠️`、`🏗?` → `🏗️`、`🗑?` → `🗑️`、`🏛?` → `🏛️`）
  - 搜索并修复所有损坏的 emoji 字符，恢复为完整的 Unicode emoji
  - 涉及行：约 2394、3055、3335、3411、4414、5783、5898、5901、6185、6215、6234、6254 行
  - 确认修复后文件以 UTF-8 编码保存
  - _需求：2.1_

- [ ] 3. 修复中文汉字截断
  - 搜索并修复所有被截断的汉字：`买不?` → `买不起`、`损?` → `损失`、`缺?` → `缺少`、`稳?` → `稳定`、`详?` → `详情`、`投资?` → `投资了`、`花?` → `花费`、`交战?` → `交战中`、`新政?` → `新政权`、`清理?` → `清理了`、`缺?` → `缺货`（注释中）
  - 涉及行：约 3095、3335、3707、3709、4414、5225、5240、5247、5898、5901、6215 行
  - 同时修复注释中的截断汉字（如 3707、3709 行），保持代码可读性
  - _需求：2.2、2.3、2.4_

- [ ] 4. 将英文死亡日志翻译为中文
  - 将 `suffered deaths from prolonged shortages of ${reason}: ${deaths}.` 改为 `因长期缺少${reason}导致${deaths}人死亡。`
  - 将资源名常量 `'food and cloth'`、`'food'`、`'cloth'` 分别改为 `'粮食和布料'`、`'粮食'`、`'布料'`
  - 涉及行：约 6438、6439 行
  - _需求：3.1、3.2_

- [ ] 5. 全文扫描验证与构建检查
  - 对 `simulation.js` 全文做最终扫描，搜索残留的 `?{`、`??`、`?` 等异常模式，确保无遗漏
  - 检查 `recordAggregatedLog` 相关消息修复后的聚合逻辑不受影响（相同事件的 key 格式保持一致）
  - 执行 `npm run build` 确认无编译错误
  - _需求：技术约束 1-5、边界情况 1-3_
