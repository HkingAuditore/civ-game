# 2026-03-09 Daily Log

## 完成：战争-经济深度联动系统（war-economy-deep-integration）

### 7个任务全部完成

1. **warEconomy.js 核心模块** (NEW)
   - `aggregateWarDamagedBuildings` — 汇总所有战线建筑损毁
   - `calculateWarBuildingDamage` — 玩家侧概率破坏建筑，AI侧扣减wealth/milStr
   - `calculateWarPopulationLoss` — 人口流失（1%-3%按区域）
   - `calculateWarPlunder` — 持续财富掠夺（经济区2%/核心区4%，入侵方获60%）
   - `calculateWartimeTradeDisruption` — 每场战争-15%贸易量，最高45%
   - `calculateMilitaryIndustryBoost` — 军事建筑+20%，采矿+10%
   - `gameConstants.js` 新增 `WAR_ECONOMY` 常量块

2. **frontSystem.js 集成**
   - `processFrontAdvance` checkpoint处调用建筑破坏和人口流失
   - `processFrontFriction` 中调用财富掠夺
   - `getFrontlineEconomicModifiers` 新增腹地沦陷层级（linePos≤8/≥92），产出惩罚上限从35%提到55%

3. **simulation.js 消费**
   - 建筑产出循环用 `effectiveBuilds`（原builds减去warDamagedBuildings）
   - 战时军工繁荣加成 + 贸易中断惩罚

4. **militaryUnits.js 军费预算制**
   - `calculateUnitCost(unitId, prices)` 基于recruitCost×nationPrices
   - `generateNationArmy()` 重构：军费预算制替代硬编码wealthConstraint
   - 兵种按epoch降序选最先进能负担的，无可用兵种的类别跳过
   - 物价低→同预算养更多兵；物价高→兵少+便宜兵种

5. **aiWar.js AI军团调度**
   - `allocateAICorpsToFronts()` 跨战线优先级分配有限军团
   - `applyAICorpsAllocation()` 执行分配结果更新corps
   - `checkWarDeclaration` 增加军团余量权衡（0空闲→95%降低宣战概率）

6. **aiEconomy.js 战时惩罚**
   - `updateAIDevelopment` 战时wealth损耗用WAR_ECONOMY常量
   - 新增 militaryStrength 战时消耗（0.002/tick）
   - `processPostWarRecovery` 用WAR_ECONOMY常量

7. **useGameLoop.js + useGameState.js 掠夺集成**
   - 消费 `processFrontAdvance` 的 `warEconomyDamage`：建筑破坏、AI经济损伤、人口流失、玩家掠夺获益
   - 消费 `processFrontFriction` 的持续掠夺：用实际AI wealth计算
   - 跨战线AI军团调度调用集成在 `syncAINationMilitary` 后
   - `useGameState.js` 新增 `pendingRepairs` 状态（save/load已处理）

### 注意事项
- `npm run build` 因预存在的 Vite 7 HTML inline-proxy 问题失败，与本次修改无关
- eslint 通过，所有报告的error均为预存在的 no-unused-vars
- frontSystem中 `calculateWarPlunder` 传入 `targetWealth: 0`，实际掠夺由useGameLoop用真实AI wealth重新计算

## 完成：军事系统 Bug 批量修复（12个Bug）

### Bug 1: 批量建造成本退化为固定值
- `buildingUpgradeUtils.js::applyBuildingCostModifier` — 新增 `minPenalty = penalty * 0.3` 保底，防止高额官员加成下数量惩罚被完全抵消

### Bug 2: 军团编入单位双倍扣除
- `CorpsManagementPanel.jsx::unassignedArmy` — 移除重复减法逻辑，直接使用 army 状态（assignUnitsToCorps 已物理移除）

### Bug 3: 编入军团后不扣军饷
- `simulation.js` — 合并 army + militaryCorps 为 allMilitaryUnits，统一计算 calculateArmyMaintenance 和 calculateArmyPopulation

### Bug 4: 军团显示"脳"乱码
- `CorpsManagementPanel.jsx` — 将"脳"替换为正确的乘号"×"

### Bug 5: 非战斗士气不减
- `simulation.js` — armyExpenseResult 新增 isUnderPaid/payRatio 字段
- `useGameLoop.js` — corps 士气更新新增欠饷被动衰减（每tick -3~-6）

### Bug 7: 军队概览面板显示0
- `MilitaryTab.jsx::totalUnits` — 统计口径扩展到包含 militaryCorps 内的单位

### Bug 8: 训练完成后丢兵
- `useGameLoop.js` — slotsAvailableForCompletion 和 setArmy 竞态检查都纳入军团兵力

### Bug 9: 招募容量突破
- `useGameActions.js` — destructure militaryCorps，getTotalArmyCount 纳入军团单位
- `useGameLoop.js` — 自动补兵和队列裁剪的容量检查也纳入军团兵力

### Bug 10: 军事同盟国不参战
- `nations.js::processMonthlyRelationDecay` — 从 diplomacyOrganizations 获取组织盟友，给予关系衰减保护
- `simulation.js` — 传入 organizations 给 processMonthlyRelationDecay

### Bug 12: 补给满足率定量→定性退化
- `useGameLoop.js` — 扣除补给后重新计算实际 supplyRatio 并更新 battleContext（不仅设 hasEnoughSupply 布尔值）

### Bug 13: 碾压优势下高死亡率
- `battleSystem.js::calculatePhaseOutcome` — 引入兵力比修正：
  - `dampenAttacker/dampenDefender = 1/sqrt(forceRatio)` 降低弱势方对强势方的损伤率
  - 动态保底战损率 `max(0.05%, 0.3% * dampen)`
  - 绝对伤害上限：一方损失不超过对方兵力的50%

### 修改文件清单
- `src/utils/buildingUpgradeUtils.js`
- `src/components/panels/CorpsManagementPanel.jsx`
- `src/logic/simulation.js`
- `src/hooks/useGameLoop.js`
- `src/hooks/useGameActions.js`
- `src/components/tabs/MilitaryTab.jsx`
- `src/logic/diplomacy/nations.js`
- `src/logic/diplomacy/battleSystem.js`
